$("h1.header")
// works the same way with css selectors
$("button");
// in jQuery don't have difference if u select one or many, this selector $ can select one button or many button 
//just with one tool.
